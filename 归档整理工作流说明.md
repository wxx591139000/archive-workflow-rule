# 归档整理自动执行规则

当说 **"归档整理"** 时，自动执行以下标准流程。

对齐 GPT 5.6 归档动作，核心产出：**生成/更新 5 份项目核心文档** + Git 归档 + 清理 + **交接准备（服务 /clear 无缝续接）**。

> 完整配套：归档完成后自动产出交接物，让你可以放心 `/clear`；下次说 **「按交接提示继续」** 即可无缝衔接任务继续推进。

---

## 流程步骤（全自动按序执行）

### 1. 代码分析（为文档生成做准备）
- 扫描项目文件结构、主要代码文件、配置文件
- 分析 README、package.json、pyproject.toml 等元数据文件获取项目信息
- 提取 Git 提交历史（`git log --oneline -30`）
- 识别项目技术栈、主要模块

### 2. Git 提交与推送
- `git add .` 暂存所有改动
- `git commit -m "归档: <项目名> <YYYY-MM-DD>"` 提交
- `git push` 推送到远程
- `git tag archive-<YYYYMMDD>` 打归档标签
- `git push --tags` 推送标签

### 3. 生成/更新 5 份核心文档 → `/docs/`

#### ① 项目目标 SPEC → `/docs/SPEC.md`
- 项目定位与目标
- 核心功能列表
- 技术栈与架构概览
- 关键接口/组件定义
- 已知约束与边界

#### ② 项目详细方案 → `/docs/ARCHITECTURE.md`
- 系统架构图（文字描述）
- 模块划分与职责
- 数据流 / 调用链路
- 关键设计决策（ADR 摘要）
- 部署架构

#### ③ 项目测试计划 → `/docs/TEST_PLAN.md`
- 测试范围与策略
- 单元测试 / 集成测试 / E2E 覆盖情况
- 测试用例清单（基于代码中的测试文件分析）
- 已知测试缺口
- 回归测试建议

#### ④ 项目踩坑记录 → `/docs/PITFALLS.md`
- 从 Git 提交信息、注释、代码中的 TODO/FIXME/HACK 提取踩坑点
- 每个踩坑记录格式：`[日期] 问题描述 → 原因 → 解决/规避方案`
- 常见陷阱与注意事项
- 性能瓶颈记录

#### ⑤ 项目推进进度 → `/docs/PROGRESS.md`
- 里程碑回顾（基于 Git tag 和提交历史）
- 已完成功能清单
- 进行中工作
- 待办事项 / Roadmap
- 归档时状态快照

### 4. 更新 README 和 CHANGELOG
- 更新 `README.md` 版本号、项目状态
- 生成/更新 `CHANGELOG.md`，追加本次归档条目

### 5. 清理构建产物
- 删除常见构建/临时目录：`build/`、`dist/`、`temp/`、`__pycache__/`、`.cache/`、`*.log`
- 注意：`node_modules/`、`.next/` 等只清理如果未被 `.gitignore` 排除

### 6. 生成归档摘要 → `/docs/archive-<YYYYMMDD>.md`
- 归档日期与标签信息
- 本次归档变更摘要
- 5 份核心文档索引链接
- Git 提交统计

### 7. 交接准备（服务 /clear 无缝续接）

> 目标：让「做个归档」后可以放心 `/clear`；下次说 **「按交接提示继续」** 时，新会话能自动加载交接记忆、对齐锚点、无缝推进。
> 关键：新会话开始时只有 `MEMORY.md` 会**自动进入上下文**。所以交接记忆必须写成 memory 文件并被 `MEMORY.md` 索引，续接才不丢锚点。

按序产出以下交接物：

① **收集交接数据**：项目名 / 本地绝对路径 / Git remote+分支+最新 tag / 当前版本 / 公网或端口（如有关联隧道，注明本地 or VPS，勿破坏隧道约定）/ 启动方式 / 遗留待办（含非阻塞）/ 硬约束
② **写「续接提示」记忆** `<项目slug>-resume-handoff.md`（type: project）：含触发说明——说「按交接提示继续」= 读此记忆 + 交接文档，对齐锚点后直接干；已存在则增量更新
③ **写「单次交接」记忆快照** `<项目slug>-handover-YYYY-MM-DD.md`（type: project）：本次归档后的可续接状态快照
④ **更新 `MEMORY.md` 索引**：为 ②③ 各加一行索引；这是续接不丢锚点的根本
⑤ **写交接文档**（人类可读，每项目独立，勿互相覆盖）：默认放**项目内** `docs/接交接.md`；多项目并存时共享文件只作「当前最近项目」指针
⑥ **收尾预告**：报告里写明——交接准备已完成，现在可 `/clear`，下次说「按交接提示继续」即可无缝续接

> **多项目隔离**：交接记忆/文档按项目 slug 命名，各自独立、互不覆盖；续接时按记忆内「项目名 + 锚点」对齐到对应项目。

---

## 收尾报告
输出归档摘要报告，包含：
- 归档标签名
- 5 份文档路径
- 文件变更统计
- 清理结果
- ★交接产物（resume-handoff / handover 记忆 / MEMORY.md 索引 / 交接文档路径）+ 续接方式提示

---

## 约束
- 如果项目没有 `/docs/` 目录，自动创建
- 如果当前目录不是 Git 仓库，跳过 Git 相关步骤，仅做文档生成与更新
- 已有文档不做全量覆盖，而是基于分析增量更新（保留用户手动添加的内容）
- 文档生成使用中文

## 触发方式
在项目目录下说 **"归档整理"** 或 **"归档"** 即自动执行上述全部流程（含交接准备）。

归档后 **`/clear`**，下次在任何项目目录说 **「按交接提示继续」**，即加载对应项目的交接记忆，无缝衔接继续推进。
